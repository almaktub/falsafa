(()=>{const entries=window.FALSAFA_ENTRIES;const labels={meaning:'Meaning',suffering:'Suffering',death:'Death',control:'Control',self:'Self & consciousness'};const questions={meaning:'What makes a life worth living?',suffering:'What do we do with pain?',death:'How should we live with an ending?',control:'What is ours to change?',self:'What do we mean by “I”?'};let params=new URLSearchParams(location.search),topic=labels[params.get('topic')]?params.get('topic'):'',view=params.get('view')==='topic'?'topic':'thinker';const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function card(e,i){return `<li class="entry-card"><a href="${e.href}"><span class="entry-number">${String(i+1).padStart(2,'0')}</span><div><p class="entry-kicker">${escape(e.kicker)}</p><h3>${escape(e.name)}</h3><p class="entry-excerpt">${escape(e.excerpt)}</p><p class="entry-tags">${e.tags.map(t=>labels[t]).join(' · ')}</p></div><span class="entry-arrow" aria-hidden="true">↗</span></a></li>`}function render(update=false){let filtered=entries.filter(e=>!topic||e.tags.includes(topic));document.querySelectorAll('[data-topic]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.topic===topic)));document.querySelectorAll('[data-view]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.view===view)));document.querySelector('#result-note').textContent=`${filtered.length} entries${topic?' on '+labels[topic].toLowerCase():''}${view==='thinker'?', arranged alphabetically.':'. Thinkers can appear under more than one question.'}`;document.querySelector('#entry-container').innerHTML=view==='thinker'?`<ul class="entry-list">${[...filtered].sort((a,b)=>a.name.localeCompare(b.name)).map(card).join('')}</ul>`:Object.keys(labels).filter(t=>!topic||topic===t).map(t=>`<section class="question-group"><p class="eyebrow">${labels[t]}</p><h2>${questions[t]}</h2><ul class="entry-list">${entries.filter(e=>e.tags.includes(t)).map(card).join('')}</ul></section>`).join('');if(update){const p=new URLSearchParams();if(topic)p.set('topic',topic);if(view==='topic')p.set('view',view);try{history.replaceState(null,'',location.pathname+(p.size?'?'+p:'')+location.hash)}catch{}}}document.querySelectorAll('[data-topic]').forEach(b=>b.addEventListener('click',()=>{topic=b.dataset.topic;render(true)}));document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>{view=b.dataset.view;render(true)}));render()})();
